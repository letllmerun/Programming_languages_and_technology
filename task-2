seconds = int(input("Enter the numbers of seconds: "))

hours = seconds // 3600
minutes = (seconds % 3600) // 60
remainig_seconds = seconds = seconds % 60

print("Hours:", hours)
print("Minutes:", minutes)
print("Remaining seconds:", remaining_seconds)
